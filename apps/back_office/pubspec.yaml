name: back_office
description: "SHIK Platform Back Office — Flutter Web menu catalog & branch management (ADR-1600 / UI-805)."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  desktop_drop: ^0.8.4
  dio: ^5.11.0
  equatable: ^2.1.0
  file_picker: ^13.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  intl: ^0.20.3
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
